{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "contacts",
            "database": "${workspaceFolder:phonebook}/db.sqlite3"
        }
    ]
}